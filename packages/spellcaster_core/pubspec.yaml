name: spellcaster_core
description: Core rules engine for Spellcaster (Waving Hands) — gesture matching, spells, turn resolution.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  collection: ^1.19.0
  meta: ^1.15.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
